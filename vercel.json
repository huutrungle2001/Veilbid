{
  "buildCommand": "pnpm --filter @veilbid/chain-bindings build && pnpm --filter @veilbid/tender-room build",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
